# One domain per line. Prefix with a dot to allow all subdomains too.
.opencode.ai

# Vertex AI inference (global + regional endpoints) and Google auth token exchange.
# Docs:
# - Vertex AI REST service endpoints include aiplatform.googleapis.com and LOCATION-aiplatform.googleapis.com
# - Service account JWT assertion audience is https://oauth2.googleapis.com/token
aiplatform.googleapis.com
oauth2.googleapis.com

# Optional providers you may enable later in .env / librechat.yaml:
.openai.com
.anthropic.com
.openrouter.ai
.tensorx.ai
generativelanguage.googleapis.com
iamcredentials.googleapis.com
sts.googleapis.com

# Brave Search API — only used when LIBRECHAT_SEARCH_PROVIDER_OVERRIDE=brave
# (see optional/local-search/compose.yml). Harmless to allow otherwise.
api.search.brave.com

# tiktoken encoding download. rag_api uses tiktoken for query tokenization
# (langchain OpenAIEmbeddings default) and downloads cl100k_base.tiktoken on
# first use; subsequent calls are cached locally. ~1.7MB one-shot.
openaipublic.blob.core.windows.net
